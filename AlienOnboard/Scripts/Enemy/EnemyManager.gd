extends Node

var _healthpoints : int


# Called when the node enters the scene tree for the first time.
#func _ready():
	#_healthpoints = 30
#
#
## Called every frame. 'delta' is the elapsed time since the previous frame.
#func _process(delta):
	#pass
#
#func _takeHit() -> bool:
	#_healthpoints -= 10;
	#var isDead:bool = _healthpoints <= 0;
	#if (isDead):
		#_Die()
	#return isDead
#
##func _Die() -> void:
	##if player != null:
		##player.queue_free()
#
